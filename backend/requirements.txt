# ================================================================
#  SUNBOW INSULATION — Backend Dependencies
# ================================================================
#  Install: pip install -r requirements.txt

# ── FastAPI core ─────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0     # ASGI server
python-multipart==0.0.9       # form data support

# ── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.35            # ORM
asyncpg==0.29.0               # async PostgreSQL driver
alembic==1.13.2               # DB migrations (use in production)

# ── Validation ───────────────────────────────────────────────
pydantic==2.9.0
pydantic-settings==2.5.0      # reads .env file
email-validator==2.2.0        # validates email format

# ── HTTP client (for WhatsApp API calls) ─────────────────────
httpx==0.27.2

# ── Security ─────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # password hashing

# ── Background tasks / Queue ─────────────────────────────────
# TODO Phase 5: uncomment when adding Celery for scheduled tasks
# celery==5.4.0
# redis==5.1.0

# ── Dev tools ────────────────────────────────────────────────
python-dotenv==1.0.1